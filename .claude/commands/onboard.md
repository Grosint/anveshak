Read CLAUDE.md at the repo root.
Then read .claude/skills/source-adapter-sdk.md.
Then read .claude/skills/llm-patterns.md.
Then run: make ps
Check if Ollama has models loaded: curl -s http://localhost:11434/api/tags
Report: which services are running, which are down, which Ollama models are loaded, and current git branch.
Then ask me what I want to work on today.
